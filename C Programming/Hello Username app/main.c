#include <stdio.h>
#include <stdlib.h>

/*
    Take user's name using Input,
    then print it on screen like "hello, user"
*/



    int main()
        {
        char answer = getchar("W");
        printf("Hello, %s", answer);
        return 0;
    }
