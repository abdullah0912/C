#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Declare a variable
    int myNum;

    // Assign a value to the variable
    myNum = 15;
    printf("My Number:" + myNum);
    return 0;
}

